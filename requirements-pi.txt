# Raspberry Pi-only dependencies. Install on the Pi with:
#   pip install -r requirements.txt -r requirements-pi.txt
-r requirements.txt

adafruit-circuitpython-ads1x15>=2.2
adafruit-blinka>=8.0
# tflite-runtime was renamed to ai-edge-litert by Google and is the only package
# that supports Python 3.12+ (Raspberry Pi OS Bookworm / Trixie).
# It installs the same tflite_runtime module so no code changes are needed.
ai-edge-litert>=1.0; platform_machine == "aarch64"
# 32-bit Pi (armv7l) has no ai-edge-litert wheel; install tflite-runtime manually
# from https://github.com/google-coral/pycoral/releases if you need 32-bit support.
